fix(backend): close concurrency loops and harden secret validation

Wrap receipt flush and commit in IntegrityError handling, eager-load
dataset document records to prevent detachment, and reject blank
SOVEREIGN_NODE_SECRET values at startup.

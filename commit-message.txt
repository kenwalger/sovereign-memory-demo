fix(api): harden question lifecycle routing and search recall

Skip airlock on empty retrieval, catch duplicate receipts at the route layer,
rank search results by keyword density, make dataset ingestion idempotent, and
pin core FastAPI/SQLAlchemy/Uvicorn dependency floors.

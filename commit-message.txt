fix(hardening): exclude WAL artifacts and tighten input boundaries

Ignore SQLite WAL/SHM sidecars in git, cap QuestionRequest length at 1000
characters, offload provenance lookups via asyncio.to_thread, and escape
LIKE wildcards in memory repository search terms.

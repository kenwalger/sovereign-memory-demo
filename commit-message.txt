fix(startup): offload dataset ingestion and tighten CORS policy

Run initialize_datasets on a worker thread during lifespan bootstrap and
restrict CORSMiddleware to explicit methods and headers while preserving
local showcase origin mappings.

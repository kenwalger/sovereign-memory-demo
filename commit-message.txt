fix(robustness): resolve concurrency, ledger leak, and error rendering

Replace non-atomic receipt ID counting with SQLite autoincrement sequences,
offload receipt persistence via asyncio.to_thread, inject a singleton
SovereignLedger into AirlockBoundary, and unpack structured FastAPI error
detail objects in the frontend API client.
